```
PhaseFieldSAV/                 # 项目根目录
├── Project.toml               # 项目依赖声明（Ferrite, Tensors, SuiteSparse 等）
├── Manifest.toml              # 依赖的具体版本记录（自动生成）
├── src/                       # 核心源代码目录
│   ├── PhaseFieldSAV.jl       # 主入口文件（模块声明、include 其他文件）
│   ├── types.jl               # 【第1层】参数定义与仿真状态数据结构
│   ├── constitutive.jl        # 【第2层】积分点层面的局部本构引擎（应变能分裂、应力计算）
│   ├── fe_context.jl          # 【第3层】标准有限元上下文（网格、DofHandler、质量/刚度矩阵组装）
│   ├── sav_integrator.jl      # 【第4层】SAV 空间积分计算、右端项组装及 $r^{n+1}$ 更新逻辑
│   └── solvers.jl             # 【第4,5层】解耦相场求解器、动力学位移求解器及主时间步循环
├── examples/                  # 经典学术基准测试用例（Examples）
│   ├── single_edge_tension.jl # 单边缺口拉伸测试 (Tension Benchmark)
│   └── single_edge_shear.jl   # 单边缺口剪切测试 (Shear Benchmark)
└── test/                      # 单元测试目录（Unit Tests）
    ├── runtests.jl            # 测试主入口
    ├── test_constitutive.jl   # 测试应变谱分解、正负能量分裂的数学一致性
    └── test_solvers.jl        # 测试 Kd 矩阵的对称正定性及极小步长下的能量衰减规律
```

+--------------------------------------------+
       |            1. Parameter & State            |  <-- 数据承载层（无状态结构体）
       +--------------------------------------------+
                             |
                             v
       +--------------------------------------------+
       |          2. Constitutive Engine            |  <-- 本构计算层（高频、无分配、Tensors.jl）
       +--------------------------------------------+
                             |
                             v
       +--------------------------------------------+
       |           3. Finite Element Context        |  <-- 有限元算子层（网格、自由度、恒定矩阵组装）
       +--------------------------------------------+
                             |
                             v
       +--------------------------------------------+
       |         4. SAV & Multi-Field Solver        |  <-- 核心求解层（SAV标量更新、相场合成、位移求解）
       +--------------------------------------------+
                             |
                             v
       +--------------------------------------------+
       |             5. Time Loop Control           |  <-- 流程控制层（时间步滚动、外推与状态更新）
       +--------------------------------------------+
       [开始时间步 n+1]
   │
   ├── 1. 外推物理场: 计算 d_bar, u_bar (Eq. 54)
   │
   ├── 2. 计算外推积分点数据:
   │       - 对应变进行正负分解 (Eq. 11)
   │       - 组装非凸力源 H_tilde (Eq. 52)
   │       - 计算积分 A^(n+1/2) (Eq. 55)
   │
   ├── 3. 组装右端项: 
   │       - 组装稀疏右端加载向量 b^n (Eq. 59)
   │
   ├── 4. 回代求解辅助场:
   │       - y1 = Kd_fact \ (Md * Dn)
   │       - y2 = Kd_fact \ bn
   │
   ├── 5. 全局合成标量与相场:
   │       - 计算标量 r^(n+1) (Eq. 64)
   │       - 合成更新节点相场向量 D^(n+1) (Eq. 60)
   │
   ├── 6. 求解位移场:
   │       - 求解基于 σ_tilde 的动力学方程，更新 u^(n+1) (Eq. 36)
   │
   ├── 7. 物理约束与截断:
   │       - 可选: 将超过 [0, 1] 物理区间的 d^(n+1) 进行局部截断 (防止微小数值越界)
   │
   └── [进入下一步 n+2]